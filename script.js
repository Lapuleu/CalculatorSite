function type (input) {
    let inputBox = document.getElementById("inputBox");
    inputBox.innerHTML+=input;
}
function solve(){
    let inputBox = document.getElementById("inputBox");
    let list = inputBox.innerText;
}